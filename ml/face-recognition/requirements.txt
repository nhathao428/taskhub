# ===================================================================
# Face recognition prototype - TaskHub (do an chuyen nganh)
# ===================================================================
# QUAN TRONG: torch / torchvision KHONG ghi phien ban cu the o day.
# Phai cai torch RIENG truoc (lenh khac nhau tuy may co GPU CUDA hay khong)
# - xem huong dan chi tiet trong README.md, muc "Buoc 2: Cai torch".
# Sau khi cai torch xong moi chay:
#   pip install -r requirements.txt

facenet-pytorch==2.6.0
# opencv-contrib-python (KHONG dung opencv-python): mediapipe tu keo opencv-contrib-python
# lam dependency - neu ghim rieng opencv-python o day, pip se cai CA HAI goi cung luc, va
# ca 2 cung ghi vao chung module "cv2" -> xung dot/loi import kho hieu tren Windows.
# opencv-contrib-python la superset cua opencv-python (co du moi ham dang dung trong code).
opencv-contrib-python==4.10.0.84
# scikit-learn KHONG con can nua: thiet ke moi la embedding-based verification
# (so cosine similarity, xem enroll.py/verify.py) thay vi train classifier SVM/kNN,
# nen bo luon dependency nay cho gon (giam rui ro xung dot version).
mediapipe==0.10.14
numpy==1.26.4
joblib==1.4.2
# Pillow PHAI trong khoang [10.2.0, 10.3.0) - facenet-pytorch==2.6.0 ghim cung: "Pillow<10.3.0,>=10.2.0".
# Ban 10.3.0+ (vd 10.4.0) se bao loi xung dot dependency luc pip install.
Pillow==10.2.0

# --- Chi can khi chay api_service.py (service HTTP cho backend Java goi sang) ---
# Neu chi chay script rieng le (capture/enroll/verify/liveness) thi khong bat buoc.
fastapi==0.115.6
uvicorn==0.34.0
pydantic==2.10.4
