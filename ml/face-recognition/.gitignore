# Anh khuon mat that va model da train la du lieu sinh trac hoc/ca nhan - KHONG commit len git.
dataset/*/
!dataset/.gitkeep

models/*
!models/.gitkeep

*.pkl
*.npz
__pycache__/
*.pyc

# Virtualenv tao khi cai dat local - khong commit (nang, khac nhau tung may)
venv/
.venv/

# train.py (classifier SVM/kNN cu) da duoc thay bang enroll.py (embedding-based
# verification) - file vat ly co the con sot lai o mot so may do gioi han xoa file
# cua moi trung, KHONG con duoc git theo doi nua, bo qua neu con thay.
train.py
