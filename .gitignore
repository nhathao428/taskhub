node_modules
__pycache__
.env
.DS_Store

# Java / Maven
target/
*.class
*.jar
*.war
*.ear
.idea/
*.iml
.project
.settings/
.classpath

# Frontend build artifacts
frontend/dist/
frontend/node_modules/

# Office temp / lock files
~$*

# PlantUML jar (download riêng – xem docs/uml/README.md)
docs/uml/plantuml.jar

# start.sh / start.ps1 runtime artefacts
.start_pids
.start_pids.txt
.logs/

# Cấu hình công cụ / editor cá nhân – không thuộc sản phẩm đồ án
.claude/
CLAUDE.md

# Phiếu .docx sinh tự động (build lại từ docs/build_phieu_docx.py) — không commit.
# Riêng báo cáo chính (.docx) và sơ đồ UML (png) được commit để xem trực tiếp
# trên GitHub; vẫn build lại được từ source khi cần.
docs/*.docx
!docs/BAO_CAO_DO_AN_CO_SO.docx
